// 9. Use trim() to remove any trailing whitespace at the beginning and the end of a string.

const randomString = "    This is a random string with trailing whitespace          "

console.log(randomString.trim()); // "This is a random string with trailing whitespace"